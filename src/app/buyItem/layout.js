
import { Fragment } from "react";

export const metadata = {
  title: "Check Out",
  description: "Check Out Counter",
};
export default function CartLayout({ children }) {
  return (
    <Fragment >
    
     {children}
   
    </Fragment>
  );
}
